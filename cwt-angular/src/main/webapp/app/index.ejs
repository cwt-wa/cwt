<!DOCTYPE html>
<html lang="en" class="h-100">
<head>
    <title><%= title %></title>
    <base href="/">
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no, viewport-fit=cover">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
    <link rel="icon" type="image/x-icon" href="/favicon.ico"/>
    <link rel="manifest" href="/manifest.json">
</head>
<body class="d-flex flex-column h-100">

<my-app>
    <div class="loading">Loading&hellip;</div>
</my-app>

<footer class="footer mt-auto bg-secondary py-2">
    <div class="container text-white small text-center text-sm-left">
        <strong><%= nameLong %></strong>
        <div class="float-sm-right">
            <% footer.forEach(function (f, idx, arr) { %>
                <a class="text-white" href="<%= f.value %>" target="_blank"><%= f.label %></a>
                <% if(idx !== arr.length-1) { %>•<% } %>
            <% }); %>
        </div>
    </div>
</footer>

</body>
</html>

